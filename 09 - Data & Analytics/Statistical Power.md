# Statistical Power

The probability that a test will correctly reject a false null hypothesis (i.e., detect an effect if it exists).

- **Target**: Usually 80%.
- **Factors**: Sample size, effect size, and alpha (significance level).